# @cloudflare/sandbox/bridge

This directory contains the bridge library exported from `@cloudflare/sandbox/bridge`. It provides the `bridge()` factory, warm pool Durable Object, API routes, and OpenAPI schema.

For user-facing documentation — API reference, deployment guide, authentication, security, and warm pool configuration — see [`bridge/worker/README.md`](../../../../bridge/worker/README.md).
