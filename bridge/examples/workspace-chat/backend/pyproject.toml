[project]
name = "workspace-chat-backend"
version = "0.1.0"
requires-python = ">=3.12,<3.13"
dependencies = [
    "starlette",
    "uvicorn[standard]",
    "openai-agents[cloudflare]",
]


[tool.ruff]
line-length = 100
